import React from 'react';

// This file is just an example component so the component file is tracked by git
// Try to make any reusable code as a component and keep them here

const ExampleComponent = () => {
  return (
    <div>
      <h2>Hello from ExampleComponent!</h2>
      <p>This is a basic example of a React component.</p>
    </div>
  );
};

export default ExampleComponent;